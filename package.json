{
  "name": "rate-limiter-monorepo",
  "private": true,
  "scripts": {
    "clean": "pnpm -r --parallel exec rm -rf dist",
    "build": "pnpm run clean && pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0",
    "tsup": "^8.0.0"
  }
}
